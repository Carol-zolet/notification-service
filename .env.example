# Database
# Desenvolvimento (SQLite - arquivo local)
# DATABASE_URL="file:./dev.db"

# Produção (PostgreSQL - Render automaticamente fornece)
# A variável DATABASE_URL será fornecida automaticamente pelo Render
# Formato: postgresql://user:password@host:port/database
DATABASE_URL="postgresql://localhost:5432/notifications?schema=public"

# Server
PORT=3001

# Frontend URL (para CORS)
# Adicione a URL do seu frontend na Vercel aqui
FRONTEND_URL=https://seu-frontend.vercel.app

# SMTP (opcional - MockEmailService não precisa)
# Para usar envio real, configure um serviço SMTP:
# SMTP_HOST=smtp.mailtrap.io
# SMTP_PORT=2525
# SMTP_USER=seu_usuario
# SMTP_PASS=sua_senha
# SMTP_FROM="Sistema de Notificações <noreply@notifications.com>"
