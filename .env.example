# Exemplo de variáveis de ambiente para deploy

DATABASE_URL=
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM=
NODE_ENV=production
FRONTEND_URL=
CORS_ORIGIN=
NOTIFICATION_WORKER_INTERVAL_MS=60000
